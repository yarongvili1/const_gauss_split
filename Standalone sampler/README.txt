The main files are 
i) master_falcon.c which implements a discrete Gaussian sampler for sigma approximately equal to 5.15543, and
ii) sample_opt.c which implements a discrete Gaussian sampler for sigma approximately equal to 2 which is used in Falcon signature algorithm

The compilation instructions are in the files.

